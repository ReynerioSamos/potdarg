<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>
        <!-- Refresh Button-->
        <form action="/refresh" method="GET">
            <button type="submit">Refresh Picture of the Day and Randomize Greeting</button>
        </form>
        
        <!-- Checks for API Response to display appropriate status-->
        <% if (error) { %>
            <div class="error">
                <%= error %>
            </div>
        <% } else if (!apodData) { %>
            <div class="loading">
                Loading today's astronomy picture...
            </div>
        <% } else { %>
            <div class="content">
                <div class="image-container">
                    <img src="<%= apodData.url %>" alt="<%= apodData.title %>" class="apod-image">
                </div>

                <!-- Random Greeting Display-->
                <div class="greeting">
                    <p class="greeting-text"><%= greeting.greeting %></p>
                    <p class="greeting-language"><em>(<%= greeting.language %>)</em></p>
                </div>
                
                <div class="info">
                    <h2 class="apod-title"><%= apodData.title %></h2>
                    <p class="apod-date">Date: <%= apodData.date %></p>
                    <p class="apod-explanation"><%= apodData.explanation %></p>
                    <% if (apodData.copyright) { %>
                        <p class="apod-copyright">© <%= apodData.copyright %></p>
                    <% } %>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html>
